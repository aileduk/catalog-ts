import React, { FC } from "react"

import styles from "./NotFound.module.scss"

const NotFound: FC = () => {
	return <div className={styles.container}>Sorry, item not found</div>
}

export default NotFound
