import React, { FC } from "react"

import styles from "./NotAvailable.module.scss"

const NotAvailable: FC = () => {
	return <div className={styles.text}>Немає в наявності</div>
}

export default NotAvailable
